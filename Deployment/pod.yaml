apiVersion: v1
kind: Pod
metadata:
  name: helloapp
  labels:
    app: helloapp
spec:
  containers:
  - name: helloapp
    image: nginx
    resources: {}
    ports:
    - containerPort: 80
      name: http
      protocol: TCP